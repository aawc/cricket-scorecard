# Task List - Stack Batsmen Vertically and Fix Active Striker Highlight

- [x] Change batsman columns in `index.html` from `col-6` to `col-12` to stack striker/non-striker vertically, giving full width on mobile screens
- [x] Add missing `id="batsman1-container"` and `id="batsman2-container"` in `index.html` to enable the active striker green highlight
- [x] Fix mock DOM `classList.add()` and `classList.remove()` in `test/test.js` to support rest parameters for multiple classes
- [x] Add Test 34 in `test/test_cases.js` to assert the striker container active state background green highlight changes with strike rotation
- [x] Bump daily sequential version to `v20260614-012` in `index.html` and `sw.js`
- [x] Run unit tests (`node test/test.js`)
- [ ] Propose commit message, get approval, and commit
