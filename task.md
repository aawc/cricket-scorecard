# Task List - Make score entry view batsman stats compact for mobile layout

- [x] Change batsman stats layout to compact `Runs (Balls)` format (e.g. `11 (12)`) in `src/ui.js`
- [x] Change bowler stats layout to compact `Wickets/Runs (Overs.Balls)` format (e.g. `1/12 (1.2)`) in `src/ui.js`
- [x] Add standing instruction in `PROMPT.md` to always write unit tests
- [x] Add Test 33 in `test/test_cases.js` to assert compact formats
- [x] Expose `updateUI` to tests in `test/test.js` and call it in `resetTestState`
- [x] Bump daily sequential version to `v20260614-011` in `index.html` and `sw.js`
- [x] Run unit tests (`node test/test.js`)
- [ ] Propose commit message, get approval, and commit
